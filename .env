# Controla se o modelo de reconhecimento de fala (ASR) sera carregado na inicializacao.
# Use "0" para desabilitar (modo texto, inicializacao rapida e sem GPU).
# Use "1" para habilitar (modo audio, requer torch + torchaudio + microfone).
ASSISTENTE_CARREGAR_MODELO=1
